# The page's own texts. This file is the default one: it is used whenever the
# request locale has no file of its own. It is saved in UTF-8 - see the "é" and
# the "—" below, which is what proves it.
title=One page, two languages
current=This request is being rendered in {0}.
explain=Nothing on this page chooses a language. Every label, every enum value \
and every text below comes from a .properties file that was picked for this \
request — the browser's language, or the one forced with the link above.
greeting=Bonjour... no, sorry: good afternoon. Have a café on us.
accented=Text that only survives a UTF-8 file: café, naïve, € 12,50, 20 °C, — an em dash.
langs=Render this page in:
fromEntity=Labels that come from the entity
fromEnum=Labels that come from an enum
fromFramework=Texts that come from the framework's own bundle
