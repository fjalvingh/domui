/** Panning Image Panel javascript code */
WebUI.PanningImagePanel = {
	attach: function(id) {
		var p = document.elementById(id);
		if(! p)
			alert('pip.internal: cannot attach to '+id);
		
		//-- register mouse listeners for drag and drop
	
	}
};
