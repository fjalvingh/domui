/**
 * Contains an alternate combobox that fully replaces the "select" html thingy. Use this in
 * preference to a select because the select cannot be styled properly.
 */
package to.etc.domui.component.combobox;
